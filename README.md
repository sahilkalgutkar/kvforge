# kvforge

I'm building kvforge, an in-memory key-value store engine written in Rust,
with a Redis-inspired wire protocol, append-only file persistence, and a CLI
client. Work in progress — full documentation lands as each piece merges.
